import { PagePlaceholder } from '../shared/ui/page-placeholder';

export function AuditLogsPage() {
  return <PagePlaceholder description="Audit logs table with filters/date range will be added next." />;
}
