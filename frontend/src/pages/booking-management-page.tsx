import { PagePlaceholder } from '../shared/ui/page-placeholder';

export function BookingManagementPage() {
  return <PagePlaceholder description="Booking management table and booking operations panel will be added next." />;
}
